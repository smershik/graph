describe("returns minimal route",function() {
	// it("from 3 to 5", function () {
	// 	assert.equal(g.searchMinRoute(3,5),29);
	// });
	// it("from 1 to 5", function () {
	// 	assert.equal(g.searchMinRoute(1,5),24);
	// });
	// it("from 4 to 5", function () {
	// 	assert.equal(g.searchMinRoute(4,5),17);
	// });
	// it("from 5 to 3", function () {
	// 	assert.equal(g.searchMinRoute(5,3),29);
	// });
	// it("from 1 to 2", function () {
	// 	assert.equal(g.searchMinRoute(1,2),9);
	// });
	// it("from 1 to 1", function () {
	// 	assert.equal(g.searchMinRoute(1,1),0);
	// });
	// it("from 1 to 999", function () {
	// 	assert.equal(g.searchMinRoute(1,999),'not existing node');
	// });

});
